import React from 'react'

import { Container } from './styles'

const Button = ({ title }) => {
  return (
    <Container>
      <button>
        <img src="" alt="" />
        {title}
      </button>
    </Container>
  )
}

export default Button
